# Simple3DViewer - 3D Визуализатор

Приложение для просмотра и редактирования 3D-моделей в формате Wavefront OBJ.

## 📋 Содержание

- [Возможности](#возможности)
- [Требования](#требования)
- [Архитектура](#архитектура)
- [Структура проекта](#структура-проекта)
- [Сборка и запуск](#сборка-и-запуск)
- [Использование](#использование)
- [API для разработчиков](#api-для-разработчиков)
- [Расширение функционала](#расширение-функционала)

## ✨ Возможности

- 📂 **Загрузка и сохранение OBJ файлов** - полная поддержка формата Wavefront OBJ
- 🎨 **Управление несколькими моделями** - добавление, удаление, переименование
- ✏️ **Редактирование геометрии** - удаление вершин и полигонов по индексам
- 🎥 **Управление камерой** - перемещение в 6 направлениях
- 🌓 **Темы оформления** - тёмная и светлая темы
- 📊 **Информация о модели** - статистика по вершинам, полигонам, нормалям
- ⚡ **Модульная архитектура** - легко расширяемый код

## 🔧 Требования

- **Java 17** или выше
- **Maven 3.6** или выше
- **JavaFX 17** (подключается автоматически через Maven)

## 🏗️ Архитектура

Проект следует принципам **Clean Architecture** и **SOLID**:

### Слои архитектуры

```
┌─────────────────────────────────────────────────┐
│                    UI Layer                      │
│  (JavaFX Controllers, FXML, CSS)                │
├─────────────────────────────────────────────────┤
│                 Service Layer                    │
│  (ModelFileService, бизнес-логика)              │
├─────────────────────────────────────────────────┤
│                  Core Layer                      │
│  (Model, Scene, Math - доменные объекты)        │
├─────────────────────────────────────────────────┤
│               Infrastructure Layer               │
│  (IO readers/writers, Render engine)            │
└─────────────────────────────────────────────────┘
```

### Паттерны проектирования

| Паттерн | Применение |
|---------|------------|
| **MVC** | Разделение UI (FXML/Controller) и логики |
| **Singleton** | `AppConfig`, `ModelFileService` |
| **Strategy** | `ModelReader`, `ModelWriter` интерфейсы |
| **Observer** | `Scene.ChangeListener` для уведомлений |
| **Factory Method** | Статические методы создания в `Polygon`, `ModelException` |

## 📁 Структура проекта

```
src/main/java/com/cgvsu/
│
├── Main.java                    # Точка входа
├── Simple3DViewer.java          # JavaFX Application
│
├── core/                        # 🔵 Ядро (доменные объекты)
│   ├── math/                    # Математические классы
│   │   ├── Vector2f.java        # 2D вектор
│   │   └── Vector3f.java        # 3D вектор
│   │
│   ├── model/                   # Модели данных
│   │   ├── IPolygon.java        # Интерфейс полигона
│   │   ├── Polygon.java         # Реализация полигона
│   │   └── Model.java           # 3D модель
│   │
│   └── scene/                   # Управление сценой
│       ├── SceneObject.java     # Объект на сцене (модель + трансформации)
│       └── Scene.java           # Контейнер объектов сцены
│
├── io/                          # 🟢 Ввод/вывод
│   ├── reader/                  # Чтение файлов
│   │   ├── ModelReader.java     # Интерфейс ридера
│   │   ├── ModelReadException.java
│   │   └── ObjModelReader.java  # OBJ ридер
│   │
│   └── writer/                  # Запись файлов
│       ├── ModelWriter.java     # Интерфейс врайтера
│       ├── ModelWriteException.java
│       └── ObjModelWriter.java  # OBJ врайтер
│
├── render/                      # 🟡 Рендеринг (для коллег)
│   └── ...
│
├── ui/                          # 🟣 Пользовательский интерфейс
│   ├── controller/              # JavaFX контроллеры
│   │   └── MainController.java
│   │
│   └── component/               # UI компоненты
│       └── DialogHelper.java    # Вспомогательные диалоги
│
├── service/                     # 🟠 Сервисный слой
│   └── ModelFileService.java    # Сервис работы с файлами
│
├── config/                      # ⚙️ Конфигурация
│   └── AppConfig.java           # Настройки приложения
│
├── exception/                   # ❌ Исключения
│   ├── ViewerException.java     # Базовое исключение
│   ├── FileOperationException.java
│   └── ModelException.java
│
└── util/                        # 🔧 Утилиты
    └── IndexParser.java         # Парсинг индексов

src/main/resources/com/cgvsu/
├── fxml/
│   └── gui.fxml                 # Разметка интерфейса
└── css/
    ├── dark-theme.css           # Тёмная тема
    └── light-theme.css          # Светлая тема
```

## 🚀 Сборка и запуск

### Сборка JAR файла

**Способ 1: Assembly Plugin (рекомендуется)**
```bash
mvn clean compile assembly:single -DskipTests=true
```
Результат: `target/Simple3DViewer.jar`

**Способ 2: Shade Plugin**
```bash
mvn clean package -DskipTests=true
```
Результат: `target/cgvsu-1.0-SNAPSHOT-executable.jar`

### Запуск

**Из IDE:**
```
Запустить класс com.cgvsu.Main
```

**Из командной строки:**
```bash
java -jar target/Simple3DViewer.jar
```

**С явным указанием JavaFX (если требуется):**
```bash
java --module-path /path/to/javafx-sdk/lib \
     --add-modules javafx.controls,javafx.fxml \
     -jar target/Simple3DViewer.jar
```

## 📖 Использование

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Ctrl+O` | Открыть OBJ файл |
| `Ctrl+S` | Сохранить активную модель |
| `Ctrl+Q` | Выход |
| `Ctrl+A` | Выбрать все модели |
| `Ctrl+R` | Сбросить камеру |
| `Delete` | Удалить выбранные вершины |
| `↑↓←→` | Перемещение камеры |
| `W` `S` | Камера вверх/вниз |

### Работа с моделями

1. **Загрузка:** Файл → Открыть или `Ctrl+O`
2. **Выбор:** Клик на модели в списке слева
3. **Множественный выбор:** `Ctrl+Click`
4. **Сохранение:** Файл → Сохранить или `Ctrl+S`

### Редактирование

Введите индексы в поле:
- Одиночные: `0, 1, 5`
- Диапазон: `5-10`
- Смешанный: `0, 3, 5-10, 15`

## 🔌 API для разработчиков

### Работа со сценой

```java
// Создание сцены
Scene scene = new Scene("MyScene");

// Подписка на изменения
scene.addChangeListener(event -> {
    System.out.println("Изменение: " + event.getType());
});

// Добавление модели
Model model = ModelFileService.getInstance().loadModel(new File("cube.obj"));
SceneObject obj = scene.addModel(model, "Куб");

// Выбор объектов
scene.selectOnly(obj);
scene.selectAll();

// Получение выбранных
List<SceneObject> selected = scene.getSelectedObjects();
```

### Работа с моделью

```java
// Создание модели
Model model = new Model("MyModel");
model.addVertex(new Vector3f(0, 0, 0));
model.addVertex(new Vector3f(1, 0, 0));
model.addVertex(new Vector3f(0, 1, 0));
model.addPolygon(Polygon.createTriangle(0, 1, 2));

// Удаление элементов
model.removeVerticesByIndices(Arrays.asList(0, 1, 5));
model.removePolygonsByIndices(Arrays.asList(0, 1, 2));

// Валидация
List<String> errors = model.validate();
boolean isValid = model.isValid();

// Статистика
Map<String, Object> stats = model.getStatistics();
```

### Чтение/запись файлов

```java
ModelFileService service = ModelFileService.getInstance();

// Загрузка
Model model = service.loadModel(new File("input.obj"));

// Сохранение
service.saveModel(model, new File("output.obj"));

// Конвертация в строку
String objContent = service.modelToString(model, "obj");
```

### Парсинг индексов

```java
// Парсинг строки индексов
List<Integer> indices = IndexParser.parse("1, 3, 5-10, 15");
// Результат: [1, 3, 5, 6, 7, 8, 9, 10, 15]

// С проверкой границ
List<Integer> valid = IndexParser.parseWithBounds("0-100", 0, 50);
// Результат: [0, 1, 2, ..., 49]

// Форматирование обратно
String formatted = IndexParser.format(Arrays.asList(1, 2, 3, 5, 6, 7, 10));
// Результат: "1-3, 5-7, 10"
```

## 🔄 Расширение функционала

### Добавление нового формата файлов

1. **Создайте ридер:**
```java
public class PlyModelReader implements ModelReader {
    @Override
    public Model read(String content) throws ModelReadException {
        // Реализация парсинга PLY
    }
    
    @Override
    public String getSupportedExtension() {
        return "ply";
    }
    // ...
}
```

2. **Зарегистрируйте:**
```java
ModelFileService.getInstance().registerReader("ply", new PlyModelReader());
ModelFileService.getInstance().registerWriter("ply", new PlyModelWriter());
```

### Добавление слушателя сцены

```java
scene.addChangeListener(event -> {
    switch (event.getType()) {
        case OBJECT_ADDED -> handleObjectAdded(event.getObject());
        case OBJECT_REMOVED -> handleObjectRemoved(event.getObject());
        case SELECTION_CHANGED -> updateUI();
    }
});
```

### Интеграция с рендером (для коллег)

```java
// Получение объектов для рендеринга
List<SceneObject> renderable = scene.getRenderableObjects();

for (SceneObject obj : renderable) {
    Model model = obj.getModel();
    Vector3f position = obj.getPosition();
    Vector3f rotation = obj.getRotation();
    Vector3f scale = obj.getScale();
    
    // Применить трансформации и отрендерить
    renderModel(model, position, rotation, scale);
}
```

## 👥 Команда

Проект разрабатывается командой из 3 человек:
- **UI/UX и Scene Management** - интерфейс, управление сценой (Есин Александр)
- **Рендеринг** - отрисовка моделей (Кобылкин Игорь)
- **Камера и трансформации** - работа с камерой (Чернышова Мария)

## 📝 Лицензия

MIT License - свободное использование в учебных и коммерческих целях.
